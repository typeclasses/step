module Pushback.StackContainer
  (
    {- * Type -} StackContainer (..),
    {- * Examples -} list, sequence,
    {- * Utilities -} zoom,
  )
  where

import Pushback.StackContainer.Type
import Pushback.StackContainer.Examples
import Pushback.StackContainer.Utilities
