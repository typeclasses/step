module Cursor.Feed
  (
    {- * Types -} Feed, FeedPlus,
    {- * Examples -} pushback, substateBuffer, privateBuffer, substate, empty,
  )
  where

import Cursor.Feed.Examples
import Cursor.Feed.Type
